#include <stdio.h>

int main()
{
	int num;

	scanf("%d", &num);

	if (num >= 90) {
		printf("a");
	}
	else if (num >= 80) {
		printf("b");
	}
	else if (num >= 70) {
		printf("c");
	}
	else if (num >= 60) {
		printf("d");
	}
	else
		printf("f");

	return 0;
}
