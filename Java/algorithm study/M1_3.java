public class M1_3 {
    public static void main(String[] args) {
        String src = "Hello Java";

        StringBuilder sb = new StringBuilder();
        System.out.println(sb.append(src).reverse());
    }
}

// str 문자열을 반대로 뒤집어라. StringBuilder에 있는 reverse()를 사용해라.
